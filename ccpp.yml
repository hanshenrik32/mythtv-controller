name: C/C++ CI

on: [push, pull_request]

jobs:
  build-linux:
    name: Linux build

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Install dependencies
      run: |
        sudo apt update
        sudo apt install -y libkrb5-dev curl xmltv libvlc-dev lirc glutg3-dev
        tar -zxvf fmodstudioapi20107linux.tar.gz
    - name: make installsound
      run: make installsound
    - name: make
      run: make compile

~